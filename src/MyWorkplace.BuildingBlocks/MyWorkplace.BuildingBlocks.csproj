<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />

    <PackageReference Include="Aspire.Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="EFCore.NamingConventions" />
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <PackageReference Include="Scalar.AspNetCore" />
    <PackageReference Include="WolverineFx.RabbitMQ" />
    <PackageReference Include="WolverineFx.EntityFrameworkCore" />
    <PackageReference Include="WolverineFx.Postgresql" />
    <!-- EN: Compiles Wolverine's generated handler code at startup (no pre-generated code to keep in sync).
         TR: Wolverine'in ürettiği handler kodunu açılışta derler (senkron tutulacak önceden üretilmiş kod yok). -->
    <PackageReference Include="WolverineFx.RuntimeCompilation" />
  </ItemGroup>

  <ItemGroup>
    <!-- EN: Generic contracts only — never the product's (ADR-026). TR: Sadece genel sözleşmeler — asla ürününkiler değil (ADR-026). -->
    <ProjectReference Include="..\MyWorkplace.Abstractions\MyWorkplace.Abstractions.csproj" />
    <!-- EN: For AddServiceModule (ADR-021). TR: AddServiceModule için (ADR-021). -->
    <ProjectReference Include="..\MyWorkplace.ServiceDefaults\MyWorkplace.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
